Installations:
1) run in the terminal `pip install psycopg[binary]` to install psycopg

To run the program:
1) run `python parser.py --json` in the terminal
2) copy paste registration form
3) press Enter
4) press Ctrl-Z
5) press Enter again

